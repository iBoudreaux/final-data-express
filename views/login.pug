extends layout 

block content 
    div.layoutspace
        div.loginContainer
            form(id="loginform" action="/userprof" method="post") 
                p.loginp Username: 
                    p
                        input(type="text" name='username')
                p.loginp Password:
                    p
                        input(type="text" name='password')
                p.loginp
                    p
                        input(type="submit" value="Login") 
                p.loginp
                        p=errorMsg
            a.createLink(href='/create') Don't have an account yet?